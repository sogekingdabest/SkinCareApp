<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SkinCare_TFM" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SkinCare_TFM" parent="Base.Theme.SkinCare_TFM" />

    <!-- Estilos para guías de captura -->
    <style name="GuidanceMessageText">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:padding">16dp</item>
        <item name="android:background">@drawable/guidance_message_background</item>
        <item name="android:elevation">4dp</item>
    </style>

    <style name="GuidanceIndicatorIcon">
        <item name="android:layout_width">16dp</item>
        <item name="android:layout_height">16dp</item>
        <item name="android:tint">@color/guidance_ready</item>
    </style>

    <style name="GuidanceIndicatorText">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">12sp</item>
        <item name="android:layout_marginStart">4dp</item>
    </style>

    <style name="GuidanceFAB" parent="Widget.Material3.FloatingActionButton.Primary">
        <item name="backgroundTint">@color/guidance_overlay_background</item>
        <item name="tint">@android:color/white</item>
        <item name="elevation">6dp</item>
    </style>

    <style name="GuidanceFAB.Mini">
        <item name="fabSize">mini</item>
    </style>

    <style name="GuidanceStatusIndicator">
        <item name="android:layout_width">32dp</item>
        <item name="android:layout_height">32dp</item>
        <item name="android:elevation">4dp</item>
    </style>

</resources>